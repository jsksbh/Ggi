import asyncio
import random
from AFROTOMusic import app
from pyrogram.types import (InlineKeyboardButton,
                            InlineKeyboardMarkup, Message)
from pyrogram import filters, Client


txt = [
" هآي تع اشب شااي • 😹💔"
        ]
@app.on_message(filters.command(["هاي"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        
thxt = [
" وعليكم السلام 🌝💜"
        ]
@app.on_message(filters.command(["السلام عليكم"], ""), group=173)

async def caesar(client: Client, message: Message):

      a = random.choice(thxt)

      await message.reply(

        f"{a}")        
     
htxt = [
" خدوني معاكم برايفت والنبي 🥺💔"
        ]

@app.on_message(filters.command(["برايفت"], ""), group=273)

async def caesar(client: Client, message: Message):

      a = random.choice(htxt)

      await message.reply(

        f"{a}")   
        
htt = [
" نزل عينك تحت كدا علشان هتخاد علي قفاك 😒❤️"
        ]

@app.on_message(filters.command(["🙄"], ""), group=373)

async def caesar(client: Client, message: Message):

      a = random.choice(htt)

      await message.reply(

        f"{a}")           
        
htx = [
" مع الف سلامه يقلبي متجيش تاني 😹💔🎶"
        ]

@app.on_message(filters.command(["سلام"], ""), group=253)

async def caesar(client: Client, message: Message):

      a = random.choice(htx)

      await message.reply(

        f"{a}")        

ht = [
" عليه الصلاه والسلام 🌝💛"
        ]

@app.on_message(filters.command(["صلي علي النبي"], ""), group=673)

async def caesar(client: Client, message: Message):

      a = random.choice(ht)

      await message.reply(

        f"{a}")

hxt = [
" نعم الله عليك 🌚❤️"
        ]

@app.on_message(filters.command(["نعم"], ""), group=2073)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")

hytxt = [
" القمر ده شبهك 🙂❤️"
        ]

@app.on_message(filters.command(["🌚"], ""), group=2173)

async def caesar(client: Client, message: Message):

      a = random.choice(hytxt)

      await message.reply(

        f"{a}")   

hgtxt = [
" بتفكر في اي 🤔"
        ]

@app.on_message(filters.command(["🤔"], ""), group=2573)

async def caesar(client: Client, message: Message):

      a = random.choice(hgtxt)

      await message.reply(

        f"{a}")   

ghtxt = [
" ضحكتك عثل زيكك ينوحيي 🌝❤️"
        ]

@app.on_message(filters.command(["😂"], ""), group=26773)

async def caesar(client: Client, message: Message):

      a = random.choice(ghtxt)

      await message.reply(

        f"{a}")   

jhtxt = [
" متزعلش بحبك 😻🤍"
        ]

@app.on_message(filters.command(["🥺"], ""), group=26473)

async def caesar(client: Client, message: Message):

      a = random.choice(jhtxt)

      await message.reply(

        f"{a}")   

ahtxt = [
" بتعيط تيب لي طيب 😥"
        ]

@app.on_message(filters.command(["😭"], ""), group=23573)

async def caesar(client: Client, message: Message):

      a = random.choice(ahtxt)

      await message.reply(

        f"{a}")   

shtxt = [
" نا عايز مح انا كمان 🥺💔"
        ]

@app.on_message(filters.command(["💋"], ""), group=29773)

async def caesar(client: Client, message: Message):

      a = random.choice(shtxt)

      await message.reply(

        f"{a}")   

dhtxt = [
" عدل وشك ونت بتكلمني 😒🙄"
        ]

@app.on_message(filters.command(["😒"], ""), group=2873)

async def caesar(client: Client, message: Message):

      a = random.choice(dhtxt)

      await message.reply(

        f"{a}")
mhtxt = [
"محات حياتي يروحي 🌝❤️"
        ]

@app.on_message(filters.command(["محح"], ""), group=2601473)

async def caesar(client: Client, message: Message):

      a = random.choice(mhtxt)

      await message.reply(

        f"{a}")   

lhtxt = [
" وانا كمان بعشقك يا روحي 🤗🥰"
        ]

@app.on_message(filters.command(["بحبك"], ""), group=231673)

async def caesar(client: Client, message: Message):

      a = random.choice(lhtxt)

      await message.reply(

        f"{a}")   

xhtxt = [
" دايما ياحبيبي 🌝❤️"
        ]

@app.on_message(filters.command(["الحمدلله"], ""), group=274683)

async def caesar(client: Client, message: Message):

      a = random.choice(xhtxt)

      await message.reply(

        f"{a}")   

dfhtxt = [
" بنهش كتاكيت احنا هنا ولا اي ??😹"
        ]

@app.on_message(filters.command(["هشش"], ""), group=2756033)

async def caesar(client: Client, message: Message):

      a = random.choice(dfhtxt)

      await message.reply(

        f"{a}")   

nhtxt = [
" هلا بيك ياروحي 👋"
        ]

@app.on_message(filters.command(["هلا"], ""), group=207973)

async def caesar(client: Client, message: Message):

      a = random.choice(nhtxt)

      await message.reply(

        f"{a}")   

phtxt = [
" وحيات امك ياكبتن خدوني معاكو بيف 🥺💔"
        ]

@app.on_message(filters.command(["بف"], ""), group=270973)

async def caesar(client: Client, message: Message):

      a = random.choice(phtxt)

      await message.reply(

        f"{a}")   

ihtxt = [
" ونجيب اشخاص 😂👻"
        ]

@app.on_message(filters.command(["خاص"], ""), group=273573)

async def caesar(client: Client, message: Message):

      a = random.choice(ihtxt)

      await message.reply(

        f"{a}")   

uhtxt = [
" انت الخير يعمري 🌝❤️"
        ]

@app.on_message(filters.command(["بخير"], ""), group=279373)

async def caesar(client: Client, message: Message):

      a = random.choice(uhtxt)

      await message.reply(

        f"{a}")   

rhtxt = [
" اه اي يا قدع عيب 😹💔"
        ]

@app.on_message(filters.command(["اه"], ""), group=267473)

async def caesar(client: Client, message: Message):

      a = random.choice(rhtxt)

      await message.reply(

        f"{a}")   

htxtk = [
"خخخ امال 😹"
        ]

@app.on_message(filters.command(["حصل"], ""), group=225973)

async def caesar(client: Client, message: Message):

      a = random.choice(htxtk)

      await message.reply(

        f"{a}")

asdhtxt = [
" لا عيب بتكسف 😹💔"
        ]

@app.on_message(filters.command(["تع"], ""), group=200873)

async def caesar(client: Client, message: Message):

      a = random.choice(asdhtxt)

      await message.reply(

        f"{a}")   

pokghtxt = [
" ده نورك ي قلبي 🌝💙"
        ]

@app.on_message(filters.command(["منور"], ""), group=200173)

async def caesar(client: Client, message: Message):

      a = random.choice(pokghtxt)

      await message.reply(

        f"{a}")   

ijkhtxt = [
" اي الثقافه دي 😒😹"
        ]

@app.on_message(filters.command(["ويت"], ""), group=200273)

async def caesar(client: Client, message: Message):

      a = random.choice(ijkhtxt)

      await message.reply(

        f"{a}")   

kghtxt = [
"ع فين لوين رايح وسايبنى 🥺💔"
        ]

@app.on_message(filters.command(["باي"], ""), group=200373)

async def caesar(client: Client, message: Message):

      a = random.choice(kghtxt)

      await message.reply(

        f"{a}")   

lphtxt = [
" اهدا يوحش ميصحش كدا 😒??"
        ]

@app.on_message(filters.command(["خخخ"], ""), group=200473)

async def caesar(client: Client, message: Message):

      a = random.choice(lphtxt)

      await message.reply(

        f"{a}")   

tthtxt = [
" العفو ياروحي 🙈🌝"
        ]

@app.on_message(filters.command(["شكرا"], ""), group=200573)

async def caesar(client: Client, message: Message):

      a = random.choice(tthtxt)

      await message.reply(

        f"{a}")   

qqhtxt = [txt = [
" انت الي حلو ياقمر 🤤🌝"
        ]

@app.on_message(filters.command(["حلوه"], ""), group=200673)

async def caesar(client: Client, message: Message):

      a = random.choice(qqhtxt)

      await message.reply(

        f"{a}")   

wwhtxt = [
" موت بعيد م ناقصين مصايب 😑😂"
        ]

@app.on_message(filters.command(["بموت"], ""), group=200773)

async def caesar(client: Client, message: Message):

      a = random.choice(wwhtxt)

      await message.reply(

        f"{a}")   

zzhtxt = [
"فرح خالتك قريب 😹💋💃🏻"
        ]

@app.on_message(filters.command(["تيب"], ""), group=200873)

async def caesar(client: Client, message: Message):

      a = random.choice(zzhtxt)

      await message.reply(

        f"{a}")   

vvhtxt = [
" جتك اوهه م سامع ولا ايي 😹👻"
        ]

@app.on_message(filters.command(["اي"], ""), group=200973)

async def caesar(client: Client, message: Message):

      a = random.choice(vvhtxt)

      await message.reply(

        f"{a}")   

xxhtxt = [
" حضرلك الخير يارب 🙂❤️"
        ]

@app.on_message(filters.command(["حاضر"], ""), group=2000173)

async def caesar(client: Client, message: Message):

      a = random.choice(xxhtxt)

      await message.reply(

        f"{a}")   

cchtxt = [
" لف ورجع تانى مشحوار 😂🚶‍♂👻"
        ]

@app.on_message(filters.command(["جيت"], ""), group=2000273)

async def caesar(client: Client, message: Message):

      a = random.choice(cchtxt)

      await message.reply(

        f"{a}")   

kjjhtxt = [
"يوه خضتني ياسمك اي 🥺💔"
        ]

@app.on_message(filters.command(["بخ"], ""), group=2000373)

async def caesar(client: Client, message: Message):

      a = random.choice(kjjhtxt)

      await message.reply(

        f"{a}")   

ffhtxt = [
" خلصتت روحكك يبعيد 😹💔"
        ]

@app.on_message(filters.command(["خلاص"], ""), group=2000473)

async def caesar(client: Client, message: Message):

      a = random.choice(ffhtxt)

      await message.reply(

        f"{a}")   

pphtxt = [
" امك اسمها احلام 😹😹"
        ]

@app.on_message(filters.command(["تمام"], ""), group=2000573)

async def caesar(client: Client, message: Message):

      a = random.choice(pphtxt)

      await message.reply(

        f"{a}")   

oohtxt = [
" اوه ياه 🌝😂"
        ]

@app.on_message(filters.command(["حبيبي"], ""), group=20703)

async def caesar(client: Client, message: Message):

      a = random.choice(oohtxt)

      await message.reply(

        f"{a}")   

llhtxt = [
" كفيه شقط سيب حاجه لغيرك 😎😂"
        ]

@app.on_message(filters.command(["سيفي"], ""), group=20713)

async def caesar(client: Client, message: Message):

      a = random.choice(llhtxt)
      
      await message.reply(

        f"{a}")   

kkhtxt = [
"كفيه شقط سيب حاجه لغيرك 😎😂"
        ]

@app.on_message(filters.command(["سي في"], ""), group=22703)

async def caesar(client: Client, message: Message):

      a = random.choice(kkhtxt)

      await message.reply(

        f"{a}")   
        
txt = [
" انا اكتر يا عمري •❤💋"
        ]
@app.on_message(filters.command(["بحبك"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
      
        txt = [
" صباح الخير يا ًوجها كان النور يغشاه فشمس في سماء الله وشمس في محياك "
        ]
@app.on_message(filters.command(["صباح الخير "], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")  
        
        txt = [
"  بـّخـْيرٌ دامـّگ بـْخـّيرٌ يـّٱلـّغـٌالـّے"
        ]
@app.on_message(filters.command(["عامل ايه "], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" وبطيب القلب مسلماً ترد السلام وعلينا وعليكم وعلي الكل السلام "
        ]
@app.on_message(filters.command(["سلام"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" يا عيني ده انصدم 😹💗"
        ]
@app.on_message(filters.command(["نعم"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
"تدوم هي الضحكه يا عمري 😍😘 "
        ]
@app.on_message(filters.command(["😂"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" كفايه كدب يا اخي 😂"
        ]
@app.on_message(filters.command(["بحبك"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" يا صبر ايوب عايز ايه يا زفت  "
        ]
@app.on_message(filters.command(["بوت"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" هي وصلت لكده 🙄"
        ]
@app.on_message(filters.command(["لا"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" صباح الخير ونسماته، يا قمر الليل ونجماته..يا روح الي يبعثلك.. وعمره وكل حياته"
        ]
@app.on_message(filters.command(["صباح الخير"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" عيب كده يا فوزيه 🙀"
        ]
@app.on_message(filters.command(["احيه"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" ليه يا كئيب يا عدو الفرحه 😹"
        ]
@app.on_message(filters.command(["انا زعلان"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" خليك ما لسه بدري 😿"
        ]
@app.on_message(filters.command(["انا ماشي "], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" شامم ريحة مؤمره "
        ]
@app.on_message(filters.command(["."], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" وبعدين بقا 😒"
        ]
@app.on_message(filters.command(["😭"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" الشارع اللي وراه 😹🤭"
        ]
@app.on_message(filters.command(["اه"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" طول ما وشك في الموضوع مش مطمن 🙄"
        ]
@app.on_message(filters.command(["اطمن"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" لا فاهم بس بستعبط 😅"
        ]
@app.on_message(filters.command(["مش فاهم"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" ولا قبلين 😒"
        ]
@app.on_message(filters.command(["وبعدين"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" ليا انا ده ❤"
        ]
@app.on_message(filters.command(["🌹"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" ميه ميه يسطا 😆"
        ]
@app.on_message(filters.command(["تمام"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" متاكد من كلامك ده 😕"
        ]
@app.on_message(filters.command(["مفيش حاجه "], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" عيب كده مش قدام الكل 😂"
        ]
@app.on_message(filters.command(["خاص"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" لا يا شيخ 😕"
        ]
@app.on_message(filters.command(["تؤتؤ"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" بّخــــَيِرً أَلًحّمِـــــدِلٌلِه"
        ]
@app.on_message(filters.command(["اخبارك"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" ايه هتديني فلوس 😍"
        ]
@app.on_message(filters.command(["ابشر"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" وعليكم الهاي 😆❤"
        ]
@app.on_message(filters.command(["هاي"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" زي السكر في الشاي 😂"
        ]
@app.on_message(filters.command(["ازاي "], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" دعوها وشانها انها هبله 😂"
        ]
@app.on_message(filters.command(["عيب"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
"مساء مليء بالحب والإحساس مبعوث لأغلى الناس "
        ]
@app.on_message(filters.command(["مساء الخير"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" الرقم المطلوب غير متاح 😂"
        ]
@app.on_message(filters.command(["الو"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" هلا وغلا  ❤😘"
        ]
@app.on_message(filters.command(["هلا"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" ده انا هعملك محضر ابن فاجره "
        ]
@app.on_message(filters.command(["الشارع اللي وراه"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" مين عصبك كده 😂"
        ]
@app.on_message(filters.command(["😡"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" شايفني بكلم حد غيرك 😒"
        ]
@app.on_message(filters.command(["انا"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" اموت في البط 😂"
        ]
@app.on_message(filters.command(["كت"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" هكدب عليك يعني 😒"
        ]
@app.on_message(filters.command(["احلف"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" اللهم صل على سيدنا محمد طب القلوب ودوائها، وعافية الأبدان وشفائها، ونور الأبصار وضيائها وعلى اله وصحبة وسلم في كل لمحة ونفس عدد ما وسعة علم الله."
        ]
@app.on_message(filters.command(["صلي علي النبي "], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" مش هعيد كلامي مرتين 😒"
        ]
@app.on_message(filters.command(["ايه"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" زي الهوا في التكييف 😀"
        ]
@app.on_message(filters.command(["كيف"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" اديني بصيت 👀"
        ]
@app.on_message(filters.command(["بص"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" ان شاء الله عنك ما فهمت 🙄💔"
        ]
@app.on_message(filters.command(["مش فاهم "], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" وانا كمان بحبك يا روحي انت 🥺"
        ]
@app.on_message(filters.command(["❤"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" حاضر جاي اهو 🚶‍♀️"
        ]
@app.on_message(filters.command(["تعالي"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" تعالي في حضن اخوك يا فواز "
        ]
@app.on_message(filters.command(["🫂"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" اي خدمه 😘💋"
        ]
@app.on_message(filters.command(["تسلم"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" حبيبي يسطا 😂"
        ]
@app.on_message(filters.command(["العفو"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
"قول اديني سامع 😸"
        ]
@app.on_message(filters.command(["اقلك حاجه "], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" حاضر 🥲💔"
        ]
@app.on_message(filters.command(["بس"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" وانت مالك 🙄"
        ]
@app.on_message(filters.command(["ليه"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" يا ريت والله الواحد اتصدع "
        ]
@app.on_message(filters.command(["بطلو رغي"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" هاكل واجيلك 😂💜"
        ]
@app.on_message(filters.command(["رايح فين"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
"كفايه شقط يسطا 😂 "
        ]
@app.on_message(filters.command(["برايفت"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" تاخد موزه 🍌"
        ]
@app.on_message(filters.command(["جعان"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" نعم يا نوحي 💋"
        ]
@app.on_message(filters.command(["يا قلبي "], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" موجود اهو 😸"
        ]
@app.on_message(filters.command(["فينك"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" لا والله 😒🙄"
        ]
@app.on_message(filters.command(["هس"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" يا عمري علي هي الغمزه 😍😘"
        ]
@app.on_message(filters.command(["😉"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" نعم ياض 😒"
        ]
@app.on_message(filters.command(["بت"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" هتخلوني اقول كلام ما يصحش _"
        ]
@app.on_message(filters.command(["هشتمك"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" مالك بس زعلان ليه 🥺"
        ]
@app.on_message(filters.command(["😔"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" طيب خليك شويه لسه السهره طويله 😂"
        ]
@app.on_message(filters.command(["يلا باي"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" ايوه والله الواحد نعسان 😪"
        ]
@app.on_message(filters.command(["هنام"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" وحياة شنبك 😹💔"
        ]
@app.on_message(filters.command(["وحياة امك "], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" امال امتي يعني 🙄😒"
        ]
@app.on_message(filters.command(["دلوقتي"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" تمام 👌 قوي يا عم "
        ]
@app.on_message(filters.command(["كده تمام "], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" وريني نفسك يا رجوله 😸😹"
        ]
@app.on_message(filters.command(["هضربك"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" هشتم 😒"
        ]
@app.on_message(filters.command(["انت قليل الادب "], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" اف منك انت 😾"
        ]
@app.on_message(filters.command(["اف"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" وربنا انت عسل 🥺😘"
        ]
@app.on_message(filters.command(["حصل خير"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" هو لسه في تاني 👀"
        ]
@app.on_message(filters.command(["كمل"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
"🙄"
        ]
@app.on_message(filters.command(["امم"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" يعني قل خير او اصمت"
        ]
@app.on_message(filters.command(["يعني"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" ومالو من حقك تشوف نفسك 😎"
        ]
@app.on_message(filters.command(["طبعا"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" يا واد يا واثق 😉"
        ]
@app.on_message(filters.command(["عيب عليك "], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" حاضر سكت اهو 🤐"
        ]
@app.on_message(filters.command(["اسكت"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" تسلم يا ابو الصحاب "
        ]
@app.on_message(filters.command(["حبيبي يسطا "], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" بالموز 😘"
        ]
@app.on_message(filters.command(["سلاموز"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" اتكلم عدل احسنلك 🔪😒"
        ]
@app.on_message(filters.command(["اوكيشن"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" بشياكه 😋"
        ]
@app.on_message(filters.command(["خلصانه"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" مالك بس بعيد الشر عنك 🥺"
        ]
@app.on_message(filters.command(["تعبان"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" هو كان في اولاني 🙃👀"
        ]
@app.on_message(filters.command(["تاني"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" بتبص فين يا جدع 😺🤩"
        ]
@app.on_message(filters.command(["👀"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" انا مالي يا عم 😒"
        ]
@app.on_message(filters.command(["انت"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" اهدي انت 😒💔"
        ]
@app.on_message(filters.command(["اهدي"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" بعشقك ❤"
        ]
@app.on_message(filters.command(["بموت فيك "], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
"مۡسَـ(🌙)ـاء الۣخيۡـ(🌌)ـݛاَت"
        ]
@app.on_message(filters.command(["مساءالورد"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" صباح الليمون علي احلي عيون 👀"
        ]
@app.on_message(filters.command(["صبـ(⛅)ـُ(آٍلـٍـً(🌺)ـٍورٍدً)ـ(⛅)ـٍآٍآٍحً"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" لف وارجع تاني 🚶‍♀️"
        ]
@app.on_message(filters.command(["انا جيت "], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" عملت ايه يا اخرة صبري 😹💔"
        ]
@app.on_message(filters.command(["😅"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" هتكدب من اولها 😂"
        ]
@app.on_message(filters.command(["ولا حاجه "], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" اجري يا مجدي 🚶‍♀️😸"
        ]
@app.on_message(filters.command(["يا لهوي"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" ينفع اجي معاكم 👀🙄"
        ]
@app.on_message(filters.command(["عايزك خاص"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" معلش يا عمري 🥲"
        ]
@app.on_message(filters.command(["الواحد تعب "], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" هاجي يا عم 😁"
        ]
@app.on_message(filters.command(["هتيجي"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" شغل انت "
        ]
@app.on_message(filters.command(["شغل"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" يدوم حمدك ونبض قلبك 💚"
        ]
@app.on_message(filters.command(["الحمدلله"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" صلي علي من جلس علي ركبته يوسي طفل مات عصفوره "
        ]
@app.on_message(filters.command(["ا"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" ليه كده طيب وانا كنت عملت ايه 😭"
        ]
@app.on_message(filters.command(["مالكش دعوه"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" صبرني يا رب 🙄😒"
        ]
@app.on_message(filters.command(["هوينا"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" امووووح حياتي 😚"
        ]
@app.on_message(filters.command(["😘"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
       
       txt = [
" اخوات اشقيه ما نخفش من البشريه 💃😻"
        ]
@app.on_message(filters.command(["اتنين"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" خليهم اتنين 😂"
        ]
@app.on_message(filters.command(["واحد"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" متاكد 😁"
        ]
@app.on_message(filters.command(["نو"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" ليك يقبر قلبي علي هذا الخجل 🤩"
        ]
@app.on_message(filters.command(["🤭"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" اعدل وشك وانت بتكلمني 🙄😒"
        ]
@app.on_message(filters.command(["😒"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" اختار الاتنين 😂"
        ]
@app.on_message(filters.command(["اه ولا لا "], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" بحبك والله 😘❤"
        ]
@app.on_message(filters.command(["☺️"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}"
        
        txt = [
" لا مهم 😒"
        ]
@app.on_message(filters.command(["مش مهم "], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" مثا مثا يا ابراهيم 🤓"
        ]
@app.on_message(filters.command(["مسا مسا يا رؤف"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" وياسمينه ونعناعه"
        ]
@app.on_message(filters.command(["فله"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" فل عليك يا غالي 😂"
        ]
@app.on_message(filters.command(["فل"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        txt = [
" خلع منكم يا عم 😂"
        ]
@app.on_message(filters.command(["راح فين "], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
